<section class="hero">
  <div class="container hero-content">
    <p class="eyebrow">Axel Pariona · aka Giggio</p>

    <h1>
        Construyo aplicaciones web modernas con React, Astro, Supabase y despliegue en producción.
    </h1>

    <p class="description">
        Soy Axel Pariona, también conocido como Giggio en mis proyectos personales.
        Estudio Ciencias de la Computación y estoy orientando mi camino al desarrollo web,
        creando aplicaciones reales con autenticación, bases de datos, seguridad,
        integraciones externas y despliegue en producción.
    </p>

    <div class="actions">
      <a href="#proyectos" class="btn primary">Ver proyectos</a>
      <a href="#contacto" class="btn secondary">Contactar</a>
    </div>

    <div class="highlights">
      <span>React</span>
      <span>Astro</span>
      <span>Supabase</span>
      <span>PostgreSQL</span>
      <span>Vercel</span>
    </div>
  </div>
</section>

<style>
  .hero {
    padding: 6rem 0 4rem;
  }

  .hero-content {
    max-width: 900px;
  }

  .eyebrow {
    margin: 0 0 1rem;
    color: var(--primary);
    font-weight: 600;
    letter-spacing: 0.04em;
    text-transform: uppercase;
    font-size: 0.85rem;
  }

  h1 {
    margin: 0;
    max-width: 850px;
    font-size: clamp(2.4rem, 7vw, 5rem);
    line-height: 1;
    letter-spacing: -0.06em;
  }

  .description {
    margin: 1.5rem 0 0;
    max-width: 720px;
    color: var(--text-muted);
    font-size: 1.1rem;
    line-height: 1.7;
  }

  .actions {
    display: flex;
    flex-wrap: wrap;
    gap: 1rem;
    margin-top: 2rem;
  }

  .btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-height: 44px;
    padding: 0 1.2rem;
    border-radius: 999px;
    font-weight: 600;
    transition: transform 0.2s ease, border-color 0.2s ease, background 0.2s ease;
  }

  .btn:hover {
    transform: translateY(-2px);
  }

  .primary {
    background: var(--primary);
    color: #020617;
  }

  .secondary {
    border: 1px solid var(--border);
    color: var(--text-main);
    background: rgba(255, 255, 255, 0.03);
  }

  .secondary:hover {
    border-color: var(--primary);
  }

  .highlights {
    display: flex;
    flex-wrap: wrap;
    gap: 0.75rem;
    margin-top: 2rem;
  }

  .highlights span {
    padding: 0.5rem 0.8rem;
    border: 1px solid var(--border);
    border-radius: 999px;
    color: var(--text-muted);
    background: rgba(255, 255, 255, 0.03);
    font-size: 0.9rem;
  }

  @media (max-width: 720px) {
    .hero {
      padding: 4rem 0 3rem;
    }

    .description {
      font-size: 1rem;
    }
  }
</style>